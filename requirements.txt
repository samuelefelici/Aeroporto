# Dipendenze della web app (backend FastAPI + parser PDF).
# Versioni pinnate per build riproducibili su Coolify/Docker.
#
# L'app NON usa più Streamlit: è una web app "normale" (backend FastAPI che
# serve una SPA statica + endpoint /api/parse). Il parsing PDF resta in Python
# (pdfplumber, robusto); pandas è stato rimosso (il parser restituisce dict).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pdfplumber==0.11.10
python-multipart==0.0.20
